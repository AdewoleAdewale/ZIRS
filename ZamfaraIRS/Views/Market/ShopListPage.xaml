<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ZamfaraIRS.Views.Market.ShopListPage"Title="SHOP LIST"
             BackgroundColor="#F9FAFB">

    <StackLayout Padding="15" Spacing="10">
        <!-- Market Header -->
        <Frame CornerRadius="20" BackgroundColor="White" Padding="15" HasShadow="False" BorderColor="#E5E7EB">
            <Label Text="{Binding SelectedMarket.Market_Plaza}" FontAttributes="Bold" FontSize="16" HorizontalOptions="Center" TextColor="#064E3B" />
        </Frame>

        <!-- Search Bar -->
        <Frame CornerRadius="12" Padding="5,0" HasShadow="False" BorderColor="#D1D5DB" BackgroundColor="White">
            <SearchBar x:Name="ShopSearchBar" 
                       Placeholder="Search by shop number_" 
                       TextChanged="OnSearchTextChanged"
                       BackgroundColor="Transparent"
                       CancelButtonColor="#064E3B" />
        </Frame>

        <Label Text="{Binding TotalFound, StringFormat='{0} shops found'}" FontSize="12" TextColor="Gray" Margin="5,0,0,0" />

        <!-- Paginated List -->
        <CollectionView ItemsSource="{Binding DisplayedShops}" 
                        RemainingItemsThreshold="5"
                        RemainingItemsThresholdReachedCommand="{Binding LoadMoreShopsCommand}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="0,5" CornerRadius="12" BorderColor="#E5E7EB" HasShadow="False" Padding="15">
                        <StackLayout Spacing="10">
                            <!-- Tap gesture to toggle expansion -->
                            <StackLayout.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ToggleExpandCommand}" CommandParameter="{Binding .}" />
                            </StackLayout.GestureRecognizers>

                            <!-- Shop Details Grid -->
                            <Grid ColumnDefinitions="50, *" RowSpacing="5">
                                <StackLayout Grid.Column="0" VerticalOptions="Center">
                                    <Image Source="{Binding ExpandIcon}" HeightRequest="24" />
                                    <Label Text="ZIRS" FontSize="10" FontAttributes="Bold" HorizontalOptions="Center" TextColor="#064E3B"/>
                                </StackLayout>

                                <Grid Grid.Column="1" RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto" ColumnDefinitions="100, *">
                                    <Label Grid.Row="0" Grid.Column="0" Text="SHOP NO" FontSize="11" TextColor="Gray" FontAttributes="Bold"/>
                                    <Label Grid.Row="0" Grid.Column="1" Text="{Binding ShopNo}" FontSize="14" FontAttributes="Bold" TextColor="#111827" />

                                    <Label Grid.Row="1" Grid.Column="0" Text="OCCUPANT" FontSize="11" TextColor="Gray" FontAttributes="Bold"/>
                                    <Label Grid.Row="1" Grid.Column="1" Text="{Binding CurrentOccupant}" FontSize="12" FontAttributes="Bold" />

                                    <Label Grid.Row="2" Grid.Column="0" Text="CATEGORY" FontSize="11" TextColor="Gray" FontAttributes="Bold"/>
                                    <Label Grid.Row="2" Grid.Column="1" Text="{Binding ShopCat}" FontSize="12" FontAttributes="Bold" />

                                    <Label Grid.Row="3" Grid.Column="0" Text="CAT AMOUNT" FontSize="11" TextColor="Gray" FontAttributes="Bold"/>
                                    <Label Grid.Row="3" Grid.Column="1" Text="{Binding Amount, StringFormat='{0:N2}'}" FontSize="12" TextColor="#DC2626" FontAttributes="Bold"/>

                                    <Label Grid.Row="4" Grid.Column="0" Text="PAID" FontSize="11" TextColor="Gray" FontAttributes="Bold"/>
                                    <Label Grid.Row="4" Grid.Column="1" Text="{Binding TotalAmtPaid, StringFormat='{0:N2}'}" FontSize="12" TextColor="#DC2626" FontAttributes="Bold"/>

                                    <Label Grid.Row="5" Grid.Column="0" Text="BALANCE" FontSize="11" TextColor="Gray" FontAttributes="Bold"/>
                                    <Label Grid.Row="5" Grid.Column="1" Text="{Binding Balance, StringFormat='{0:N2}'}" FontSize="12" TextColor="#DC2626" FontAttributes="Bold"/>

                                    <Label Grid.Row="6" Grid.Column="0" Text="LAST DATE" FontSize="11" TextColor="Gray" FontAttributes="Bold"/>
                                    <Label Grid.Row="6" Grid.Column="1" Text="{Binding LastPaymentDate}" FontSize="12" TextColor="#0284C7" FontAttributes="Bold"/>
                                </Grid>
                            </Grid>

                            <!-- Expandable Button -->
                            <Button Text="VIEW SHOP DETAILS" 
                                    IsVisible="{Binding IsExpanded}"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ViewDetailsCommand}" 
                                    CommandParameter="{Binding .}"
                                    BackgroundColor="#064E3B" 
                                    TextColor="White" 
                                    CornerRadius="8" 
                                    HeightRequest="45"
                                    Margin="0,10,0,0"/>
                        </StackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>